/**
 * DEPRECATED — Supabase sudah tidak digunakan.
 * File ini dipertahankan sebagai stub kosong agar tidak ada import error
 * pada file yang belum dimigrasikan. Hapus setelah semua migrated.
 *
 * Semua akses data sekarang melalui lib/api.ts → Laravel REST API → MySQL.
 */

// Ekspor dummy 'supabase' agar import lama tidak meledak
export const supabase = null as any;
export default supabase;
